## 仓库配置包
1. https://dev.mysql.com/downloads/repo/apt/
2. 下载 mysql-apt-config_..._all.deb
3. 运行 sudo dpkg -i mysql-apt-config_*.deb
## 配置仓库
1. 选择正确的版本，并按向右方向键再回车
2. 回到主界面后，按向下方向键选中 OK 再回车
## 安装包
1. sudo apt update
2. sudo apt install libmysqlclient-dev
## 